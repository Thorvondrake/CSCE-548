# Use Amazon Corretto (a stable, production-ready distribution of OpenJDK)
FROM amazoncorretto:17-al2-jdk

# Set the working directory in the container
WORKDIR /app

# Copy everything from your repo into the container
COPY . .

# Create bin folder and compile with the JDBC driver in the lib folder
RUN mkdir -p bin && javac -cp ".:lib/*" src/*.java -d bin

# Run the app
CMD ["java", "-cp", "bin:lib/*", "App"]